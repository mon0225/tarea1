import React from 'react';

const RandomButton = ({handleClick}) => (
    
    <div>
        <p uk-margin>
            <button class="uk-button uk-button-default" onClick={this.handleClick}>Add Random Contact</button>
        </p>
    </div>
);

export default RandomButton;